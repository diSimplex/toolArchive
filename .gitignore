tmp
*.swp
*.pdf

# cnClient dependencies which are available on the web
node_modules

# our binaries
cnSetup/cnSetup
cnSetup/ca
cnSetup/servers
cnSetup/users
cnTypeSetter/cnTypeSetter
cnNursery/cnNursery

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/
